name: Release image & bump Helm values

on:
  push:
    tags:
      - "v*"
  workflow_dispatch:

permissions:
  contents: write
  packages: write

env:
  IMAGE_NAME: ghcr.io/${{ github.repository_owner }}/kubeapp
  CHART_VALUES_FILE: helm-chart/values.yaml
  BASE_BRANCH: main

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    outputs:
      image-digest: ${{ steps.build.outputs.digest }}
      image-tag: ${{ steps.vars.outputs.tag }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Derive tag from ref
        id: vars
        run: |
          TAG="${GITHUB_REF#refs/tags/}"
          echo "tag=${TAG}" >> "$GITHUB_OUTPUT"

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: Set up Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Docker metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.IMAGE_NAME }}
          tags: |
            type=raw,value=${{ steps.vars.outputs.tag }}
            type=sha,format=short

      - name: Build and push (multi-arch)
        id: build
        uses: docker/build-push-action@v6
        with:
          context: .
          file: ./Dockerfile
          platforms: linux/amd64,linux/arm64
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

  bump-helm:
    runs-on: ubuntu-latest
    needs: build-and-push
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Install yq
        uses: mikefarah/yq@v4.44.3

      - name: Show BEFORE (debug)
        run: |
          echo "---- BEFORE ----"
          sed -n '1,200p' "${{ env.CHART_VALUES_FILE }}" || true

      - name: Update Helm values.yaml (repo + tag + digest)
        id: bump
        env:
          IMAGE_NAME: ${{ env.IMAGE_NAME }}
          TAG: ${{ needs.build-and-push.outputs.image-tag }}
          DIGEST: ${{ needs.build-and-push.outputs.image-digest }}
        run: |
          set -euo pipefail
          FILE="${CHART_VALUES_FILE}"

          # Ensure image keys exist
          yq -i '(.image //= {})' "$FILE"
          yq -i '(.image.repository //= "")' "$FILE"
          yq -i '(.image.tag //= "")' "$FILE"
          yq -i '(.image.pullPolicy //= "IfNotPresent")' "$FILE"
          yq -i '(.image.digest //= "")' "$FILE"

          # Set values
          yq -i '.image.repository = strenv(IMAGE_NAME)' "$FILE"
          yq -i '.image.tag = strenv(TAG)' "$FILE"
          yq -i '.image.digest = strenv(DIGEST)' "$FILE"

          # Sanity: prevent "docker pull ..." in repository
          if yq '.image.repository' "$FILE" | grep -qi '^docker pull '; then
            echo "ERROR: .image.repository must not include 'docker pull'." >&2
            exit 1
          fi

          echo "tag=$TAG" >> "$GITHUB_OUTPUT"
          echo "digest=$DIGEST" >> "$GITHUB_OUTPUT"

      - name: Show AFTER (debug)
        run: |
          echo "---- AFTER ----"
          sed -n '1,200p' "${{ env.CHART_VALUES_FILE }}"

      - name: Prepare branch and push changes
        run: |
          set -euo pipefail

          BASE="${BASE_BRANCH}"
          BRANCH="chore/bump-helm-${{ steps.bump.outputs.tag }}"
          FILE="${CHART_VALUES_FILE}"

          git config --global user.name "github-actions[bot]"
          git config --global user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git config --global --add safe.directory "$GITHUB_WORKSPACE"

          # Ensure we base from latest BASE branch (avoid detached HEAD from tag push)
          git fetch origin "$BASE" --depth=1
          git checkout -B "$BASE" "origin/$BASE"

          # Create or update the chore branch
          if git ls-remote --exit-code --heads origin "$BRANCH" >/dev/null 2>&1; then
            git fetch origin "$BRANCH" --depth=1
            git checkout -B "$BRANCH" "origin/$BRANCH"
            git rebase "origin/$BASE" || true
          else
            git checkout -b "$BRANCH"
          fi

          # Stage and commit only if changed
          git add "$FILE"
          if git diff --cached --quiet; then
            echo "No changes to commit in $FILE (already up to date)."
          else
            git commit -m "chore(chart): bump image to ${{ steps.bump.outputs.tag }} @ ${{ steps.bump.outputs.digest }}"
          fi

          # Push branch (switch to PAT if branch protection blocks bot pushes)
          git push origin "$BRANCH"
        env:
          BASE_BRANCH: ${{ env.BASE_BRANCH }}
          CHART_VALUES_FILE: ${{ env.CHART_VALUES_FILE }}
